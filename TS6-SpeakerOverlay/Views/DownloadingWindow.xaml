<Window x:Class="TS6_SpeakerOverlay.Views.DownloadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TsOverlay - Atualizando"
        Width="320" Height="160"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#292B30"/>
            <Setter Property="Foreground" Value="#5865F2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="4" HorizontalAlignment="Left">
                                <Border.Effect>
                                    <DropShadowEffect Color="#5865F2" BlurRadius="8" Opacity="0.4" ShadowDepth="0"/>
                                </Border.Effect>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="#232428" BorderBrush="#303238" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="8" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24">
            <TextBlock x:Name="TxtTitle" Text="☁️ Downloading update..." FontSize="15" FontWeight="SemiBold"
                       Foreground="White" HorizontalAlignment="Center" TextAlignment="Center" FontFamily="Segoe UI"/>
            <ProgressBar x:Name="ProgressBarControl" Minimum="0" Maximum="100" Width="240" Height="8" Margin="0,18,0,10"/>
            <TextBlock x:Name="TxtPercent" Text="0%" Foreground="#A3A6AA" HorizontalAlignment="Center" FontSize="13" FontWeight="SemiBold"/>
        </StackPanel>
    </Border>
</Window>