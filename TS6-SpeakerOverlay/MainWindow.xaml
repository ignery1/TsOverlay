<Window x:Class="TS6_SpeakerOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TS6_SpeakerOverlay.ViewModels"
        xmlns:helpers="clr-namespace:TS6_SpeakerOverlay.Helpers"
        xmlns:models="clr-namespace:TS6_SpeakerOverlay.Models"
        Title="TS6 Overlay" Height="600" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Left="{Binding Config.WindowLeft, Mode=TwoWay}"
        Top="{Binding Config.WindowTop, Mode=TwoWay}">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <helpers:SpacingConverter x:Key="SpacingConverter"/>
        <helpers:BoolToHorizontalAlignmentConverter x:Key="AnchorConverter"/>
        <!-- Ícone de mic desativado -->
        <GeometryGroup x:Key="IconMicOff" FillRule="Nonzero">
            <RectangleGeometry Rect="9,2,6,10" RadiusX="3" RadiusY="3"/>
            <RectangleGeometry Rect="11.25,14.5,1.5,5"/>
            <RectangleGeometry Rect="7.5,19.5,9,1.6" RadiusX="0.8" RadiusY="0.8"/>
            <PathGeometry Figures="M3,4.2 L5.4,1.8 L21,17.4 L18.6,19.8 Z"/>
        </GeometryGroup>

        <!-- Ícone de áudio (headset) desativado -->
        <GeometryGroup x:Key="IconHeadsetOff" FillRule="Nonzero">
            <CombinedGeometry GeometryCombineMode="Intersect">
                <CombinedGeometry.Geometry1>
                    <CombinedGeometry GeometryCombineMode="Exclude">
                        <CombinedGeometry.Geometry1>
                            <EllipseGeometry Center="12,12" RadiusX="9" RadiusY="9"/>
                        </CombinedGeometry.Geometry1>
                        <CombinedGeometry.Geometry2>
                            <EllipseGeometry Center="12,12" RadiusX="7.4" RadiusY="7.4"/>
                        </CombinedGeometry.Geometry2>
                    </CombinedGeometry>
                </CombinedGeometry.Geometry1>
                <CombinedGeometry.Geometry2>
                    <RectangleGeometry Rect="0,2,24,10"/>
                </CombinedGeometry.Geometry2>
            </CombinedGeometry>
            <RectangleGeometry Rect="2,11,4,7" RadiusX="2" RadiusY="2"/>
            <RectangleGeometry Rect="18,11,4,7" RadiusX="2" RadiusY="2"/>
            <PathGeometry Figures="M3,4.2 L5.4,1.8 L21,17.4 L18.6,19.8 Z"/>
        </GeometryGroup>

        <!-- Ícone de "ausente" -->
        <CombinedGeometry x:Key="IconAway" GeometryCombineMode="Exclude">
            <CombinedGeometry.Geometry1>
                <EllipseGeometry Center="12,12" RadiusX="8" RadiusY="8"/>
            </CombinedGeometry.Geometry1>
            <CombinedGeometry.Geometry2>
                <EllipseGeometry Center="16.5,8.8" RadiusX="6.6" RadiusY="6.6"/>
            </CombinedGeometry.Geometry2>
        </CombinedGeometry>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="L" Modifiers="Control" Command="{Binding ToggleLockStateCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
    </Window.InputBindings>

    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="{Binding Config.UiScale}" ScaleY="{Binding Config.UiScale}"/>
        </Grid.LayoutTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. 通知 -->
        <ItemsControl ItemsSource="{Binding Notifications}" Grid.Row="0" Margin="10,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel VerticalAlignment="Bottom"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#D9000000" CornerRadius="6" Margin="0,2" Padding="12,6" BorderThickness="1" BorderBrush="#33FFFFFF">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,15,0,2" To="0,2,0,2" Duration="0:0:0.25" DecelerationRatio="0.9"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" Foreground="{Binding Color}" FontSize="16" Margin="0,0,8,0" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Message}" Foreground="White" FontSize="13" FontWeight="Medium" FontFamily="Segoe UI"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- 2. 用户列表区域 -->
        <Grid Grid.Row="1">
            <!-- 列表容器 -->
            <Border x:Name="UsersHoverZone" Background="Transparent" CornerRadius="8">
                <Grid>
                    <ItemsControl ItemsSource="{Binding Users}" Margin="8">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="UserCard" Background="#BF1E1E1E" CornerRadius="6" 
                                        Margin="{Binding DataContext.Config.ItemSpacing, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource SpacingConverter}}"
                                        Padding="10,6" BorderThickness="2" BorderBrush="Transparent"
                                        HorizontalAlignment="{Binding DataContext.Config.AnchorRight, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource AnchorConverter}}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border x:Name="AvatarContainer" Width="{Binding DataContext.Config.AvatarSize, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding DataContext.Config.AvatarSize, RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0"
                                                Cursor="Hand" ToolTip="Clique para acessar as configurações"
                                                MouseLeftButtonDown="Avatar_Click">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DataContext.Config.AvatarDisplayMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="None">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Border x:Name="AvatarBg" CornerRadius="20">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{Binding NameColor}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding DataContext.Config.AvatarDisplayMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="Indicator">
                                                                    <Setter Property="Background" Value="#72767d"/>
                                                                </DataTrigger>
                                                                <MultiDataTrigger>
                                                                    <MultiDataTrigger.Conditions>
                                                                        <Condition Binding="{Binding DataContext.Config.AvatarDisplayMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="Indicator"/>
                                                                        <Condition Binding="{Binding IsTalking}" Value="True"/>
                                                                    </MultiDataTrigger.Conditions>
                                                                    <Setter Property="Background" Value="#4FCD8E"/>
                                                                </MultiDataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                                <TextBlock Text="{Binding Initials}" Foreground="White" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding AvatarUrl}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding DataContext.Config.AvatarDisplayMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="Indicator">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <Border x:Name="AvatarImage" CornerRadius="20">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding DataContext.Config.AvatarDisplayMode, RelativeSource={RelativeSource AncestorType=Window}}" Value="Indicator">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Border.Background>
                                                        <ImageBrush ImageSource="{Binding AvatarUrl}" Stretch="UniformToFill"/>
                                                    </Border.Background>
                                                </Border>
                                                <Border x:Name="SpeakingRing" CornerRadius="20" BorderThickness="2" BorderBrush="Transparent"/>
                                            </Grid>
                                        </Border>
                                        <TextBlock Text="{Binding Name}" Foreground="#EEE" FontSize="{Binding DataContext.Config.FontSize, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" FontFamily="Segoe UI" VerticalAlignment="Center" Margin="0,0,8,0">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1" Opacity="0.8"/>
                                            </TextBlock.Effect></TextBlock>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Grid Margin="2,0" ToolTip="Microfone Desativado">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsInputMuted}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Path Data="{StaticResource IconMicOff}" Fill="#FF453A" Width="16" Height="16" Stretch="Uniform">
                                                    <Path.Effect>
                                                        <DropShadowEffect Color="#FF453A" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                                                    </Path.Effect>
                                                </Path>
                                            </Grid>
                                            <Grid Margin="2,0" ToolTip="Áudio Desativado">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsOutputMuted}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Path Data="{StaticResource IconHeadsetOff}" Fill="#FFD60A" Width="16" Height="16" Stretch="Uniform">
                                                    <Path.Effect>
                                                        <DropShadowEffect Color="#FFD60A" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                                                    </Path.Effect>
                                                </Path>
                                            </Grid>
                                            <Grid Margin="2,0" ToolTip="Ausente">
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsAway}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Grid.Style>
                                                <Path Data="{StaticResource IconAway}" Fill="#5E5CE6" Width="15" Height="15" Stretch="Uniform">
                                                    <Path.Effect>
                                                        <DropShadowEffect Color="#5E5CE6" BlurRadius="6" ShadowDepth="0" Opacity="0.6"/>
                                                    </Path.Effect>
                                                </Path>
                                            </Grid>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding IsTalking}" Value="True">
                                        <Setter TargetName="UserCard" Property="BorderBrush" Value="#4FCD8E"/>
                                        <Setter TargetName="UserCard" Property="Background" Value="#E6202225"/>
                                        <Setter TargetName="SpeakingRing" Property="BorderBrush" Value="#4FCD8E"/>
                                        <Setter TargetName="AvatarContainer" Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#4FCD8E" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsTalking}" Value="False">
                                        <Setter TargetName="UserCard" Property="Opacity" Value="{Binding DataContext.Config.BackgroundOpacity, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding DataContext.Config.ShowOnlyTalking, RelativeSource={RelativeSource AncestorType=Window}}" Value="True"/>
                                            <Condition Binding="{Binding IsTalking}" Value="False"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter TargetName="UserCard" Property="Visibility" Value="Collapsed"/>
                                    </MultiDataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="#FF5555" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,15,0,0" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ConnectionStatus}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Border>
            <!-- Ícone de configurações (aparece no hover, some quando travado) -->
            <Button x:Name="BtnSettings" Click="BtnSettings_Click"
                    Width="26" Height="26" Margin="0,6,6,0"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Cursor="Hand" ToolTip="Settings">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Opacity" Value="0"/>
                        <Setter Property="IsHitTestVisible" Value="False"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=UsersHoverZone}" Value="True"/>
                                    <Condition Binding="{Binding IsOverlayLocked}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <MultiDataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.EnterActions>
                                <MultiDataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.ExitActions>
                                <Setter Property="IsHitTestVisible" Value="True"/>
                            </MultiDataTrigger>
                            <!-- Clique em cima do avatar de alguém na sala também revela o ícone -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsSettingsIconVisible}" Value="True"/>
                                    <Condition Binding="{Binding IsOverlayLocked}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <MultiDataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.EnterActions>
                                <MultiDataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.ExitActions>
                                <Setter Property="IsHitTestVisible" Value="True"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#99000000" CornerRadius="13">
                            <TextBlock Text="⚙" Foreground="#DCDDDE" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- 拖拽背景框 -->
            <Border x:Name="DragFrame" IsHitTestVisible="False" Background="#99000000" BorderBrush="#4FCD8E" BorderThickness="1" CornerRadius="8" Margin="4">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Opacity" Value="0"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=UsersHoverZone}" Value="True"/>
                                    <Condition Binding="{Binding IsOverlayLocked}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <MultiDataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.EnterActions>
                                <MultiDataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.ExitActions>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <TextBlock Text="✥" Foreground="#4FCD8E" FontSize="24" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 3. 底部提示 (恢复) -->
        <Border Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Background="#AA000000" CornerRadius="4" Padding="8,3" Margin="10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOverlayLocked}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{DynamicResource Lang_Unlocked}" Foreground="#FFAB53" FontSize="11" FontWeight="Bold" FontFamily="Segoe UI"/>
        </Border>
    </Grid>
</Window>